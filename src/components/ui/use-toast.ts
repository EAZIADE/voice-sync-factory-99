
// Re-export toast from the hooks folder
import { useToast, type ToasterToast, type ToasterToastProps } from "@/hooks/use-toast";

export { useToast };
export type { ToasterToast, ToasterToastProps };
